version: "2"

linters:
  default: none
  enable:
    - gologlinter
  
  settings:
    custom:
      gologlinter:
        type: "module"
        description: linter for golang logs

run:
  timeout: 5m

issues:
  max-issues-per-linter: 0
  max-same-issues: 0